{
	"name": "companion",
	"version": "1.24.1",
	"description": "Calendar, task board, reminders and a live time tracker for notes tagged meeting, reminder, task, event, invoice or time.",
	"main": "main.js",
	"scripts": {
		"build": "node esbuild.config.mjs production"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"esbuild": "^0.28.2",
		"obsidian": "^1.5.7",
		"typescript": "^5.4.0"
	}
}
